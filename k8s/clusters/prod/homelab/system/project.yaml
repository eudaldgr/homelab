apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: system
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: system
    app.kubernetes.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  description: System components managed through GitOps
  sourceRepos:
    - '*'
  destinations:
    - server: '*'
      namespace: '*'
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"
  roles:
    - name: admin
      description: Full permissions
      policies:
        - p, proj:system:admin, applications, *, *, allow
    - name: readonly
      description: Read-only access
      policies:
        - p, proj:system:readonly, applications, get, *, allow
  syncWindows:
    - kind: allow
      schedule: '* * * * *'
      duration: 24h
      applications:
        - '*'
      manualSync: true
  orphanedResources:
    warn: true
