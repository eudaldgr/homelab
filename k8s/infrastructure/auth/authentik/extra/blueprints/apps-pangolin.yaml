# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
# yamllint disable-file
# prettier-ignore-start
# eslint-disable
---
version: 1
metadata:
  name: Apps - Pangolin
entries:
  - id: pangolin_provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: eudald.gr/pangolin
    attrs:
      authorization_flow:
        !Find [
          authentik_flows.flow,
          [slug, "default-provider-authorization-implicit-consent"],
        ]
      signing_key:
        !Find [
          authentik_crypto.certificatekeypair,
          [name, "authentik Self-signed Certificate"],
        ]
      invalidation_flow:
        !Find [
          authentik_flows.flow,
          [slug, "default-provider-invalidation-flow"],
        ]
      client_type: confidential
      grant_types:
        - authorization_code
        - refresh_token
      redirect_uris:
        - url: https://pangolin.eudald.gr/auth/idp/1/oidc/callback
          matching_mode: strict
          redirect_uri_type: authorization
      client_id: !Env PANGOLIN_CLIENT_ID
      client_secret: !Env PANGOLIN_CLIENT_SECRET
      access_code_validity: minutes=1
      access_token_validity: hours=1
      refresh_token_validity: days=30
      sub_mode: hashed_user_id
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]

  - id: pangolin_application
    model: authentik_core.application
    identifiers:
      slug: pangolin
    attrs:
      name: Pangolin
      group: Infrastructure
      meta_description: Tunnel reverse proxy
      provider: !KeyOf pangolin_provider
      policy_engine_mode: any
