apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: omada-tlsroute
  namespace: omada
spec:
  parentRefs:
    - name: tls-passthrough
      namespace: gateway
  hostnames:
    - "omada.home.eudald.gr"
  rules:
    - backendRefs:
        - name: omada-controller
          port: 8043
