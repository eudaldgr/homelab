image:
  pullPolicy: IfNotPresent

config:
  rootless: true
  timezone: Europe/Madrid
  showServerLogs: true
  showMongoDBLogs: false
  tlsSecretName: omada-tls

service:
  type: LoadBalancer
  annotations:
    lbipam.cilium.io/ips: "10.1.42.155"
    external-dns.alpha.kubernetes.io/hostname: omada.eudald.gr
    external-dns.alpha.kubernetes.io/target: "10.1.42.155"
    external-dns.alpha.kubernetes.io/ttl: "300"

persistence:
  data:
    enabled: true
    storageClassName: ceph-block
    size: 2Gi
  logs:
    enabled: true
    storageClassName: ceph-block
    storageClass: ceph-block
    size: 1Gi

resources:
  requests:
    cpu: 100m
    memory: 2Gi
  limits:
    cpu: 750m
    memory: 3Gi

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

startupProbe:
  httpGet:
    path: /login
    port: manage-https
    scheme: HTTPS
  failureThreshold: 60
  periodSeconds: 30

readinessProbe:
  httpGet:
    path: /login
    port: manage-https
    scheme: HTTPS
  initialDelaySeconds: 60
  periodSeconds: 15
  timeoutSeconds: 10
  failureThreshold: 3

livenessProbe:
  exec:
    command:
      - /bin/sh
      - -ec
      - |
        mounted="$(openssl x509 -in /cert/tls.crt -noout -fingerprint -sha256)"
        served="$(openssl s_client -connect 127.0.0.1:8043 -servername omada.eudald.gr </dev/null 2>/dev/null | openssl x509 -noout -fingerprint -sha256)"
        test "$mounted" = "$served"
  initialDelaySeconds: 120
  periodSeconds: 15
  timeoutSeconds: 10
  failureThreshold: 3
