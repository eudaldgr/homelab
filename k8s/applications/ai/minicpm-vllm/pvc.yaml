apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minicpm-vllm-cache
  namespace: minicpm-vllm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-csi
  resources:
    requests:
      storage: 10Gi
