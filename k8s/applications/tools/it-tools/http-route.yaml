apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: it-tools
  namespace: it-tools
spec:
  parentRefs:
    - name: internal
      namespace: gateway
  hostnames:
    - tools.k8s.eudald.gr
  rules:
    - backendRefs:
        - name: it-tools
          port: 80
