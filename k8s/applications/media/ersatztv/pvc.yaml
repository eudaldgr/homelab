apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ersatztv-config
  namespace: ersatztv
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: synology-iscsi-nvme
  resources:
    requests:
      storage: 5Gi
