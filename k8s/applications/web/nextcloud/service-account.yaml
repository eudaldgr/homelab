apiVersion: v1
kind: ServiceAccount
metadata:
  name: nextcloud-app
  namespace: nextcloud
  labels:
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/part-of: nextcloud
automountServiceAccountToken: false
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nextcloud-components
  namespace: nextcloud
  labels:
    app.kubernetes.io/name: nextcloud-components
    app.kubernetes.io/part-of: nextcloud
automountServiceAccountToken: false
