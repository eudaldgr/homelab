apiVersion: batch/v1
kind: Job
metadata:
  name: nextcloud-aio-storage-bootstrap
  namespace: nextcloud
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
    homelab.eudald.gr/security-justification: Root is used once to reset and initialize the empty CSI application volume and NFS data directory.
spec:
  backoffLimit: 6
  template:
    metadata:
      labels:
        app.kubernetes.io/component: storage-bootstrap
    spec:
      serviceAccountName: nextcloud-aio
      automountServiceAccountToken: false
      restartPolicy: OnFailure
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: initialize-nfs-directories
          image: ghcr.io/nextcloud-releases/aio-alpine:20260702_083546
          command:
            - /bin/sh
            - -eu
            - -c
          args:
            - |
              rm -rf /aio-app/* /aio-app/.[!.]* /aio-app/..?*
              rm -rf /aio-data/nextcloud/data
              mkdir -p /aio-data/nextcloud/data
              chown 33:33 /aio-app /aio-data/nextcloud/data
              chmod 0770 /aio-app /aio-data/nextcloud/data
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
              add: [CHOWN, FOWNER]
          volumeMounts:
            - name: aio-app
              mountPath: /aio-app
            - name: aio-data
              mountPath: /aio-data
      volumes:
        - name: aio-app
          persistentVolumeClaim:
            claimName: nextcloud-aio-nextcloud
        - name: aio-data
          persistentVolumeClaim:
            claimName: nextcloud-aio-nextcloud-data
