[[stack]]
name = "tailscale"
description = "Stack Tailscale"
deploy = true
tags = ["prod"]

[stack.config]
server_id = "ds920plus"
linked_repo = "homelab"
run_directory = "compose/ds920plus/prod/tailscale"
auto_pull = true
send_alerts = true
ignore_services = ["chown"]

compose_cmd_wrapper = """
/opt/sops exec-env compose.env '[[COMPOSE_COMMAND]]'"""